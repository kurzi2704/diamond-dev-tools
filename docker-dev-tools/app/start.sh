#!/bin/sh

echo "This container is designed to act as base container. This Container does nothing now, but stays open for debugging purposes. execute as docker run -ti diamond-dev-tools-base to debug into the container"

bash